; Dial plan — hands every call on extension 1000 to our ARI app (Stasis)
; No audio recording/playback here — all audio is handled via ExternalMedia RTP.

[incoming]
exten => 1000,1,Answer()
exten => 1000,n,Stasis(baat_bot)
exten => 1000,n,Hangup()
